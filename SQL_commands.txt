SQL notes

CREATE TABLE Meets (
    meetID INTEGER PRIMARY KEY AUTOINCREMENT,
    location TEXT
);
INSERT INTO sqlite_sequence (name, seq) VALUES ('Meets', 99);

CREATE TABLE Events (
    eventID INTEGER PRIMARY KEY AUTOINCREMENT,
    type TEXT,
    name TEXT,
    meetID INTEGER,
    FOREIGN KEY (meetID) REFERENCES Meets(meetID)
);
INSERT INTO sqlite_sequence (name, seq) VALUES ('Events', 999);

CREATE TABLE Heats (
    heatID INTEGER PRIMARY KEY AUTOINCREMENT,
    eventID INTEGER,
    heatNumber INTEGER,
    FOREIGN KEY (eventID) REFERENCES Events(eventID)
);
INSERT INTO sqlite_sequence (name, seq) VALUES ('Heats', 9999);

CREATE TABLE Athletes (
    athleteID INTEGER PRIMARY KEY AUTOINCREMENT,
    surname TEXT,
    givenName TEXT,
    team TEXT,
    dob DATE
);

INSERT INTO sqlite_sequence (name, seq) VALUES ('Athletes', 99999);